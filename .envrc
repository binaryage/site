# Environment management via mise
# mise is a modern polyglot version manager (Ruby, Node.js, etc.) with excellent IDE integration
# Install: brew install mise
# Versions are specified in .tool-versions file

# SETUP REQUIRED: Enable mise activation in your shell!
# See README.md "Automated mise Activation" section for detailed instructions.
#
# Quick setup:
#   Zsh:  echo 'eval "$(mise activate zsh)"' >> ~/.zshrc && source ~/.zshrc
#   Bash: echo 'eval "$(mise activate bash)"' >> ~/.bashrc && source ~/.bashrc
#
# OR use direnv integration:
#   brew install direnv
#   Add direnv hook to your shell (see README.md)
#   Run: direnv allow

# Add any project-specific environment variables below
# Example:
# export RAILS_ENV=development
# export DATABASE_URL=postgresql://localhost/myapp_dev
