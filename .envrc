# Ruby version management via rbenv + direnv
# rbenv is a simple Ruby version manager with excellent IDE integration
# Install: brew install rbenv ruby-build
use rbenv

# Add any project-specific environment variables below
# Example:
# export RAILS_ENV=development
# export DATABASE_URL=postgresql://localhost/myapp_dev
